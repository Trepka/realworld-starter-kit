spring:
  application:
    name: real-world
  datasource:
    url: jdbc:postgresql://localhost:6543/real_world_db
    username: postgres
    password: postgres
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port : 8081